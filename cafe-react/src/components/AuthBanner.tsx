// Archivo: AuthBanner.tsx
// Componente marcador: stub para mantener compatibilidad con importaciones.
// Si en el futuro se requiere mostrar un banner de autenticación,
// reemplazar este stub por la implementación correspondiente.
import React from 'react';

// Componente vacío; devuelve null para no renderizar nada por ahora.
const AuthBanner: React.FC = () => null;

export default AuthBanner;
